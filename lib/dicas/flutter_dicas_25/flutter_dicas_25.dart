import 'package:flutter/material.dart';

class FlutterDicas25 extends StatelessWidget {
  const FlutterDicas25({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
      backgroundColor: Colors.black,
    );
  }
}
